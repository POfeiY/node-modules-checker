/**
 * This is the entry point that we bundles the Rollup into a single file.
 *
 * The dist is located at `runtime/webcontainer-server.mjs` by `pnpm run wc:prepare`.
 *
 * The dist will be send to WebContainer to create the server to communicate with the main app.
 */
